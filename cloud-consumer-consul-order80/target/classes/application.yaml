server:
  port: 80

spring:
  application:
    name: consul-provider-order
  cloud:
    consul:
      port: 8500
      host: localhost
      discovery:
        service-name: ${spring.application.name}